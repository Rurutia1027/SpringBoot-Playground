type Query {
    books: [Book]
#    define query here
    bookById(id: Int): Book
}

type Book {
    id: ID,
    name: String,
    pageCount: Int
    author: Author
}

type Author {
    id: ID
    name: String
}